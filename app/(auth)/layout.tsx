import React from "react";
import type { LayoutProps } from "@/core/types/shared/common";

export default function Layout({ children }: LayoutProps) {
  return (
    <>
      {children}
    </>
  )
}
